# 秘密鍵をコミット・pushしてはいけない理由
## 1. 秘密鍵とは何か？
   秘密鍵やAPIキーは、いわば「システムの裏口を開けるためのマスターキー」です。
   これがあれば、誰でもあなた（あるいは会社）になりすまして
   サービスを利用したり、サーバーにアクセスしたりできてしまいます。
## 2. Pushすることで起こりうる被害
　 もし誤って公開リポジトリ（GitHubなど）にPushしてしまうと、以　 下のような深刻な被害が発生する可能性があります。

* **高額な利用料金の請求**
* AWSやGoogle CloudなどのAPIキーが盗まれると、犯人がそのキ 　 　ーを使って高スペックなサーバーを勝手に大量起動し、仮想通貨のマ　イニングなどに悪用します。その結果、数日で数百万円単位の請求が　来るケースが実際に発生しています。

* **個人情報・機密状況の漏洩**
* データベースへのアクセスキーが漏洩すれば、顧客データや社外秘の　ソースコードがすべて盗み出される可能性があります。

* **システムの破壊・乗っ取り**
* サーバーの操作権限を奪われ、データが削除されたり、不正なプログ　ラム（ウイルス）を仕込まれたりします。

# `git add .`や　`git add -A`の隠れた危険性
　便利に使われる git add . ですが、実は意図しないファイルまでス　テージング（コミットの準備）に含めてしまうリスクがあります。

* **意図しないファイルまで含めてしまう**
* `git add .`の`.`を使うと、現在いるディレクトリ以下の全ての変　更したファイルについて含まれる可能性があります。そのため、push
　時に意図していない変更や機密情報まで公開される可能性がありま　
　す。`git add -A`の`-A`を使うと、ディレクトリ以下だけではなく　全ての変更が反映されるので気を付ける必要があります。
　
# 意図しないファイルまで含めないための対策

* **`git status`で確認する**
コミット前には、必ず git status を実行して、自分がどのファイルをコミットしようとしているか確認する癖をつけましょう。
$ git status
On branch main
Changes to be committed:
  (use "git restore --staged <file>..." to unstage)
	new file:   src/main.py
	new file:   .env  <-- ここで「あ、これ入れたらダメだ！」と気づくことが重要
$

# 機密情報を守るための２つの対策

* **`.gitignore`の徹底活用**

Gitに追跡させたくないファイルは、あらかじめ .gitignore ファイルに記述しておきます
。
# 環境設定ファイルを無視
.env
# 秘密鍵ファイルを無視
*.pem
# キャッシュやログを無視
__pycache__/
*.log

* **環境変数の利用**
ソースコード内に直接キーを書く（ハードコードする）のではなく、環境変数から読み込むようにプログラムを記述します
。

$import os$

# ソースコード上にはキーを書かず、環境変数から取得する
API_KEY = os.getenv("SERVICE_API_KEY")

# もし秘密鍵をコミット・pushしてしまったら？（対処の流れ）
万が一、誤って秘密鍵を公開してしまった場合は、以下の手順で迅速に対応する必要があります。

* **鍵の無効化（最優先）**
Pushした瞬間にボットが鍵を盗んでいる可能性があります。Gitの操作より先に、まず**該当のAPIキーや秘密鍵をサービス側（AWS、GitHub、Google等）で無効化（Revoke）**してください。

* **新しい鍵の発行**
無効化した鍵に代わる、新しい鍵を発行して設定し直します。

* **Git履歴の修正**
単にファイルを削除して再度コミットするだけでは、過去の履歴から鍵を閲覧できてしまいます。
git filter-repo などのツールを使い、リポジトリの全履歴から該当ファイルを完全に消去するか、リポジトリを新しく作り直す必要があります。


